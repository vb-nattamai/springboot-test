<!-- Generated by AgentReady (https://github.com/vb-nattamai/agent-ready) | Do not edit manually -->
<!-- Platform: Claude Code / Anthropic -->
<!-- Regenerate with: python scripts/run_transformer.py --target . -->

# CLAUDE.md — springboot-test

## Project Identity

This is **springboot-test** — A Kotlin, Python project.

Built with **Kotlin, Python, Java, TypeScript, Go** using **Spring Boot**.

---

## Critical Commands

```bash
# Install dependencies
# <TODO: verify>

# Build the project
# <TODO: verify>

# Run the project
# <TODO: verify>

# Run all tests
# <TODO: verify>

# Run a single test file
{{SINGLE_TEST_COMMAND}}

# Lint and format
{{LINT_COMMAND}}
{{FORMAT_COMMAND}}
```

---

## Repository Map

```
{{DIRECTORY_TREE}}
```

### Key Files You'll Work With Most

{{#KEY_FILES}}
- **`{{PATH}}`** — {{DESCRIPTION}}
{{/KEY_FILES}}

---

## Rules — ALWAYS Follow

1. **Never modify** files in `{{PROTECTED_DIRECTORIES}}`
2. **Always run** `# <TODO: verify>` after making changes
3. **Always run** `{{LINT_COMMAND}}` before committing
4. **Use** {{NAMING_CONVENTION}} for all new files and symbols
5. **Follow** the existing import ordering convention
6. **Add tests** for every new function or endpoint
7. **Keep functions small** — max 50 lines per function
8. **Use typed parameters** — no `any` types allowed

## Rules — NEVER Do

1. ❌ Never `console.log` / `print` for debugging in committed code
2. ❌ Never commit secrets, tokens, or credentials
3. ❌ Never modify auto-generated files ({{AUTO_GENERATED_FILES}})
4. ❌ Never use `*` imports unless the module is designed for it
5. ❌ Never skip error handling — always handle errors explicitly
6. ❌ Never reference paths that don't exist in the repository

---

## Architecture

{{ARCHITECTURE_DESCRIPTION}}

### Data Flow

{{DATA_FLOW_DESCRIPTION}}

### Key Patterns

{{#PATTERNS}}
- **{{PATTERN_NAME}}:** {{PATTERN_DESCRIPTION}}
{{/PATTERNS}}

---

## When Adding a New Feature

1. Create the feature file in `{{SOURCE_DIR}}`
2. Add types/interfaces in `{{TYPES_DIR}}`
3. Write tests in ``
4. Update the relevant documentation
5. Run `# <TODO: verify>` to verify
6. Run `{{LINT_COMMAND}}` to check formatting

## When Fixing a Bug

1. Write a failing test that reproduces the bug
2. Fix the code
3. Verify the test passes
4. Check for related edge cases
5. Run the full test suite

## When Refactoring

1. Ensure test coverage exists for the code being refactored
2. Make changes incrementally
3. Run tests after each change
4. Verify no behavior changes (unless intentional)

---

## API Endpoints

{{#API_ENDPOINTS}}
### `{{METHOD}} {{PATH}}`
- **Description:** {{DESCRIPTION}}
- **Handler:** `{{HANDLER}}`
- **Auth:** {{AUTH_REQUIRED}}
- **Request:** {{REQUEST_SCHEMA}}
- **Response:** {{RESPONSE_SCHEMA}}

{{/API_ENDPOINTS}}

---

## Environment Variables

{{#ENV_VARS}}
- `{{NAME}}` — {{DESCRIPTION}} ({{REQUIRED_OR_OPTIONAL}}, default: `{{DEFAULT}}`)
{{/ENV_VARS}}

---

## Common Pitfalls

{{#PITFALLS}}
- ⚠️ **{{PITFALL_TITLE}}:** {{PITFALL_DESCRIPTION}}
{{/PITFALLS}}

---

## Testing

- **Framework:** unknown
- **Directory:** ``
- **Run all:** `# <TODO: verify>`
- **Run single:** `{{SINGLE_TEST_COMMAND}}`
- **Coverage:** `{{COVERAGE_COMMAND}}`

Test files follow the pattern: `{{TEST_FILE_PATTERN}}`
